:ruby
  load 'lib/rds_sql_server/rds.rb'

- if ! Rds.rds_available? || ENV['NO_LSA_RDS'] == 'true'
  -# This should only show up if you are working locally or haven't configured RDS correctly
  .alert.alert-danger
    The LSA requires access to an additional SQL server to run.  This configuration does not appear to be setup correctly.  Please reach out to your system administrator.
