- content_for :title, 'Adults with Children Dashboard'
- @show_census = false
%h1= content_for :title
- if @html
  = render 'dashboards/tabs', active: 'adults_with_children'
%p.text-muted
  Clients enrolled in homeless projects (ES, SH, SO, TH) where ...