%h1.h3.mb-4= @title
%form{method: 'post', id: page_config.form_id, novalidate: true}
  - @definition_nodes.each do |node|
    = HmisExternalApis::StaticPages::FormGenerator.new(self).render_node(node).html_safe
  = render_form_actions
