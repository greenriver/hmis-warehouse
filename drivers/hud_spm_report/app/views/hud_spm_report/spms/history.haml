= render 'breadcrumbs'
- content_for :title, @generator.generic_title
%header.mb-3.d-flex
  %h1= content_for :title
  .ml-auto
    = link_to 'Generate New Full SPM Report', new_hud_reports_spm_path, class: 'btn btn-secondary'

= render 'hud_reports/year_tabs'
= render 'hud_reports/reports_wrapper'
