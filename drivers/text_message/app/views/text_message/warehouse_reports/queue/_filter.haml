= simple_form_for @filter, as: :filters, url: text_message_warehouse_reports_queue_index_path, method: :get do |f|
  - content_for :filters_col_full do
    .row
      .col-sm-3
        = f.input :start, as: :date_picker, label: 'Start', input_html: { value: @filter.start }
      .col-sm-3
        = f.input :end, as: :date_picker, label: 'End'

  - content_for :filter_actions do
    = f.submit 'Update Filter', class: ['btn', 'btn-primary']

  = render 'warehouse_reports/filters', f:f
