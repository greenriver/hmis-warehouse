- content_for :modal_title, 'Add New Goal'

= simple_form_for @goal, url: client_health_pctp_careplan_goals_path(@client, @careplan), remote: true do |f|
  = render 'form', f: f

  .form__actions.form__actions--right.mb-6
    = f.submit 'Save', class: 'btn btn-primary'
