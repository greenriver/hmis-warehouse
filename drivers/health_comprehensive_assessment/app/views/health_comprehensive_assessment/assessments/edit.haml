= simple_form_for @assessment, url: client_health_comprehensive_assessment_assessment_path(@client, @assessment) do |f|
  = render 'form', f: f

  .form__actions.form__actions--right.mb-6
    = f.submit 'Save', class: 'btn btn-primary'
