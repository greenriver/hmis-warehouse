- content_for :modal_title, 'Update Treatment Information'

= simple_form_for @treatment, url: client_health_comprehensive_assessment_assessment_sud_treatment_path(@client, @assessment, @treatment), remote: true do |f|
  = render 'form', f: f

  .form__actions.form__actions--right.mb-6
    = f.submit 'Save', class: 'btn btn-primary'
