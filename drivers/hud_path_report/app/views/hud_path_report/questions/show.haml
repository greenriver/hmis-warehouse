= content_for :crumbs do
  = link_to path_for_reports do
    = "« #{report_short_name} Reports"
- content_for :title do
  = "#{report_short_name} #{@question}"

%h1= content_for :title

= render 'hud_path_report/shared/report_form', url: path_for_questions(@question)
- if @pagy.count.positive?
  = render 'hud_reports/year_tabs'
  .jReportListing
    = render 'hud_reports/reports'
  = render 'hud_reports/list_refresher'
