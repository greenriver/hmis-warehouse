- content_for :title, 'All Clients Dashboard'
- @show_census = false
%h1= content_for :title
- if @html
  = render 'dashboards/tabs', active: 'clients'
%p.text-muted
  Clients enrolled in homeless projects (ES, SH, SO, TH).