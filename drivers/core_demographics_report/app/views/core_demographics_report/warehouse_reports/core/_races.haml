- reports = [@report]
- column_class = 'col-sm-8'
- if @report.include_comparison?
  - reports << @comparison
  - column_class = 'col-sm-6'
.row
  - reports.each_with_index do |report, index|
    - comparison_class = ''
    - if reports.count > 1
      - comparison_class = "c-swatch__border-left--td-#{index}"
    %div{class: column_class}
      %h3 Race Breakdowns
      .card.mb-4{class: comparison_class}
        %table.table
          %thead
            %tr
              %th Race
              %th Count
              %th Percentage
          %tbody
            - report.race_buckets.each do |id, title|
              %tr
                %th= title
                %td= link_to_if can_view_clients?, number_with_delimiter(report.race_count(id)), details_core_demographics_report_warehouse_reports_core_index_path(link_params.merge(key: "race_#{id}")), target: :_blank
                %td= number_to_percentage(report.race_percentage(id), precision: 2)
