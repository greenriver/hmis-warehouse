%h3 Number of Times on the Streets, ES, or SH in The Past 3 Years
%p Data comes from the HUD Entry Assessment of most recent enrollment within the date range for each client client.
.card.mb-4
  %table.table
    %thead
      %tr
        %th Response
        %th Count
        %th Percentage
    %tbody
      - ::HUD.yes_no_missing_options.each do |id, title|
        %tr
          %th= title
          %td= number_with_delimiter(@report.times_on_street_count(id))
          %td= number_to_percentage(@report.times_on_street_percentage(id), precision: 2)

%h3 Number of Months on the Streets, ES, or SH in The Past 3 Years
%p Data comes from the HUD Entry Assessment of most recent enrollment within the date range for each client client.
.card.mb-4
  %table.table
    %thead
      %tr
        %th Response
        %th Count
        %th Percentage
    %tbody
      - ::HUD.month_categories.each do |id, title|
        %tr
          %th= title
          %td= number_with_delimiter(@report.months_on_street_count(id))
          %td= number_to_percentage(@report.months_on_street_percentage(id), precision: 2)

%h3 Prior Living Situation
%p Data comes from the HUD Entry Assessment of most recent enrollment within the date range for each client client.
.card.mb-4
  %table.table
    %thead
      %tr
        %th Response
        %th Count
        %th Percentage
    %tbody
      - ::HUD.living_situations.each do |id, title|
        %tr
          %th= title
          %td= number_with_delimiter(@report.prior_living_situations_count(id))
          %td= number_to_percentage(@report.prior_living_situations_percentage(id), precision: 2)
