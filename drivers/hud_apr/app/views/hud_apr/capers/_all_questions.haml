.well
  %p
    The CAPER contains 22 questions, labeled 4 through 26, skipping 18.  Each question contains one or more tables of data.  Questions can be run individually below, or can be run all together.  Each report run can be downloaded as a zip file suitable for upload to HUD by following the link in the
    %strong Status
    column on subsequent pages.
  %p
    = link_to hud_reports_caper_path(0, generator: 0) do
      See the history of all report runs and download zip files
  %p
    = link_to edit_hud_reports_caper_path(0, generator: 0) do
      Generate New Full CAPER
= render 'hud_apr/shared/all_questions_table'
