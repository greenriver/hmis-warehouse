= content_for :crumbs do
  = link_to path_for_reports do
    = "« #{report_short_name} Reports"
- content_for :title do
  = "#{report_short_name} #{@question}"

%h1= content_for :title

= render 'hud_apr/shared/report_form', url: path_for_questions(report_id: 0, question: @question)

- if @reports.present?
  %p= paginate @reports
.jReportListing
  = render 'hud_apr/shared/reports'
= render 'hud_apr/shared/list_refresher'
