- if @reports.present?
  - if @question
    %h3 The Following Reports Include #{@question}
  - else
    %h3 Reports
  .card
    %table.table.table-striped
      %thead
        %tr
          %th
          %th Status
          %th{style: 'width: 50%;'} Limits
          %th Included Questions
          %th Delete
      %tbody
        - @reports.each do |report|
          %tr
            %td
              - if report.running?
                = render 'hud_apr/shared/spinner'
            %td
              = link_to_if report.completed?, report.current_status, path_for_report(report)
              .text-muted.description Run by: #{report.user.name}
            %td= render 'hud_apr/shared/parameters', report: report
            %td
              - report.build_for_questions&.each do |q|
                - if report.remaining_questions.include?(q)
                  .btn.btn-sm.btn-secondary.btn-disabled.mb-2.ml-2
                    = q
                - else
                  = link_to q, path_for_question_result(report_id: report.id, id: q), class: 'btn btn-secondary btn-sm mb-2 ml-2'
            %td
              - unless report.running?
                = link_to path_for_report(report), method: :delete, class: 'btn btn-sm btn-danger btn-icon-only', data: { confirm: 'Are you sure you want to delete this report?' }  do
                  %i.icon-cross
