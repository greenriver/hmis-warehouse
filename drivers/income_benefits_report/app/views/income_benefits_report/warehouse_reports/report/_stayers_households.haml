- reports = [@report]
- column_class = 'col-sm-8'
- if @report.include_comparison?
  - reports << @comparison
  - column_class = 'col-sm-6'
%h3 Stayer Households
.row
  - reports.each_with_index do |report, index|
    - comparison_class = ''
    - if reports.count > 1
      - comparison_class = "c-swatch__border-left--td-#{index}"
    %div{class: column_class}
      .card.mb-4{class: comparison_class}
        %table.table.table-striped.mb-0
          %thead
            %th
            %th Count
            %th Percent
          %tbody
            - report.stayer_households_data.each do |title, row|
              %tr
                %th
                  %i.icon-info{data: {toggle: :tooltip, title: row[:description]}}
                  = title
                %td= number_with_delimiter(row[:count])
                %td= percentage(row[:percent]) unless row[:percent].nil?
