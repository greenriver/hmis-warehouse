#!/usr/bin/env ruby
require 'pathname'

# path to your application root.
APP_ROOT = Pathname.new File.expand_path('../../',  __FILE__)

Dir.chdir APP_ROOT do
  puts "== Installing dependencies =="
  system "gem install bundler --conservative"
  system "bundle check || bundle install --without seven_zip"

  ymls = [
    "config/database.yml",
    "config/secrets.yml",
    "config/eto_api.yml",
    "config/exception_notifier.yml"
    "config/mail_account.yml"
    "config/weather.yml"
  ]
  ymls.each do |file|
    unless File.exist?(file)
      puts "\n== Creating #{file} =="
      system "cp #{file}.sample #{file}"
    end
  end
  
  puts "\n== Preparing Database =="
  system "bin/rake db:create"
  system "bin/rake db:migrate"
  puts "\n== Preparing Warehouse =="
  system "bin/rake grda_warehouse:create"
  system "bin/rake grda_warehouse:seed_data_sources"
  puts "\n== Seeding Database =="
  system "bin/rake db:seed"

  puts "\n== Removing old logs and tempfiles =="
  system "rm -f log/*"
  system "rm -rf tmp/cache"

  puts "\n== Restarting application server =="
  system "touch tmp/restart.txt"
end
