.report-parameters
  - options.each do |k,v|
    - next if v.blank? || v == ['']
    .report-parameters__parameter
      %label.label.label-default.parameter-label= report.key_for_display(k)
      %label.label.label-primary.parameter-value= report.value_for_display(k, v)
