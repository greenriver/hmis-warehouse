- title = "#{GrdaWarehouse::Config.currrent_health_emergency_tracing_title} Medical Restrictions"
- content_for :title, title
.row
  .col-sm-8
    %h1= title
  .col-sm-4.d-flex
    = link_to({action: :index, format: :pdf}.merge(filter: filter_params), {class: 'btn btn-secondary ml-auto'}) do
      %i.icon-download2
      Report as PDF

%p Find clients with a test result within the date range who was seen most recently at the chosen projects.
= render '/warehouse_reports/breadcrumbs', tab: 'health-emergency'
= render 'warehouse_reports/health_emergency/filter'

= render 'table'