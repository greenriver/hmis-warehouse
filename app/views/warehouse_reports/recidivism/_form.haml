= simple_form_for @filter, as: :filter, url: warehouse_reports_recidivism_index_path, html: { method: :get } do |f|
  = f.error_notification
  .row.margin-bottom-02
    .col-sm-3
      .input-group.date{data: {provide: :datepicker}}
        = f.input :start, input_html: {value: @filter.start, class: 'form-control jStart'}, label: false
        %span.input-group-addon.icon-calendar
    .col-sm-3
      .input-group.date{data: {provide: :datepicker}}
        = f.input :end, input_html: {value: @filter.end, class: 'form-control jEnd'}, label: false
        %span.input-group-addon.icon-calendar
    .col-sm-3
      = f.button :submit, value: 'Update View'
      -# link_to( params.merge( format: :xlsx ), { class: 'btn btn-primary pull-right jXls', data: { toggle: 'tooltip', title: 'download the data currently displayed' } } ) do
        Excel
