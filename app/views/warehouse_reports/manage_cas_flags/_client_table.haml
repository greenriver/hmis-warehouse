- if clients.present? && clients.any?
  %table.table.table-sm
    %thead
      %tr
        %th ID
        %th Name
    %tbody
      - clients.each do |client|
        %tr
          %td= client[:id]
          %td= link_to_if can_view_clients?, "#{client[:FirstName]} #{client[:LastName]}", appropriate_client_path(client[:id])
- else
  %p No clients found.
