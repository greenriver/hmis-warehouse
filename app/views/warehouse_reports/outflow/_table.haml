%table.table.table-hover
  %thead
    %tr
      %th Metric
      %th Clients
  %tbody
    - @report.metrics.each do |key, label|
      %tr
        %td= link_to label, details_warehouse_reports_outflow_index_path(filter: params[:filter], key: key), data: {loads_in_pjax_modal: true}
        %td= @report.send(key).count
