- title = "Partner Dashboard - #{@report.range.first} to #{@report.range.last}"
- content_for :title, title
%h1= content_for :title
= render '/warehouse_reports/breadcrumbs', tab: 'health-partner-performance'

= render 'form'

= render 'report_download', export: @pdf_export

= render 'table', entities: @agencies, detail_path: [:detail, :warehouse_reports, :health_agency_performance, :index], permission: current_user.has_some_patient_access?
