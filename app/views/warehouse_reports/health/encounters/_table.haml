%table.table.table-sm.table-hover
  %thead
    %tr
      %th Start Date
      %th End Date
      %th Completed At
      %th Delete
  %tbody
    - @reports.each do |report|
      %tr
        %td= link_to_if report.completed_at?, report.start_date, warehouse_reports_health_encounter_path(report, format: :xlsx)
        %td= link_to_if report.completed_at?, report.end_date, warehouse_reports_health_encounter_path(report, format: :xlsx)
        %td= report.completed_at
        %td
          = link_to warehouse_reports_health_encounter_path(report), method: :delete, class: 'btn btn-danger btn-sm btn-icon-only', data: {confirm: "Are you sure you want to delete this report?"} do
            %i.icon-cross
