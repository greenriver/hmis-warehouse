- title = "Patient Encounters Export"
- content_for :title, title
%h1= content_for :title
= render '/warehouse_reports/breadcrumbs', tab: 'health-general'

= render 'filter'

- if @reports.any?
  = render 'table'
