= simple_form_for :filter, method: :get do |f|
  - content_for :filters_col_full do
    .row
      .col-sm-4
        = f.input :aco, collection: Health::AccountableCareOrganization.active, selected: @aco, include_blank: '', label: 'ACO name', as: :select_two
      .col-sm-4
        = f.input :year, label: 'Report Period', collection: report_periods, selected: @report_year, as: :select_two

  - content_for :filter_actions do
    = f.submit 'Update Report', class: 'btn btn-primary mt-no-label'

  = render 'warehouse_reports/filters', f:f, submit_button: nil