- if @files.any?
  %h2 Previous 834 Files
  %table.table.table-hover
    %thead
      %tr
        %th Upload Date
        %th
        %th New Enrollments
        %th Returning Enrollments
        %th Disenrollments
    %tbody
      - @files.each do |file|
        %tr
          %td= file.created_at
          %td
            - if file.new_patients.nil?
              = render 'spinner'
          %td= file.new_patients
          %td= file.returning_patients
          %td= file.disenrolled_patients
