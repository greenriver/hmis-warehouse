
= simple_form_for @filter, as: :steps, url: warehouse_reports_cas_decision_efficiency_index_path, html: { method: :get } do |f|
  - content_for :filters_col_full do
    %p Limit to matches created within a date range.
    .row
      .col-sm-3
        = f.input :start, as: :date_picker, label: 'Start Date'
      .col-sm-3
        = f.input :end, as: :date_picker, label: 'End Date'
      .col-sm-6
        = f.input :route, collection: @filter.available_routes, input_html: {value: @filter.route, class: [:jTrigger]}, as: :select_two
    .row
      .col-sm-6.jDependent
        = f.input :first, label: 'From Completion of', collection: @filter.ordered_steps.keys, include_blank: false, input_html: { id: 'first-step' }, as: :select_two
      .col-sm-6.jDependent
        = f.input :second, label: 'To Completion of', collection: @filter.ordered_steps[@filter.first], include_blank: false, input_html: { id: 'second-step' }, as: :select_two
      -# .col-sm-2
      -#   = f.input :unit, label: 'In', collection: @filter.units.map{|m| [m.titleize.pluralize, m]}, include_blank: false, input_html: { class: 'jUnits '}, as: :select_two
  - content_for :filter_actions do
    = f.submit 'Update', class: ['btn', 'btn-primary']

  = render 'warehouse_reports/filters', f:f
