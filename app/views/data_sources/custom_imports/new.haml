- content_for :title, "New Custom Import for #{@data_source.short_name}"
%h1= content_for :title
.row
  .col-sm-8
    = simple_form_for @config, as: :config, url: data_source_custom_imports_path(@data_source) do |f|
      = render 'form', f: f
      = f.button :submit, value: 'Add Import Config'
