- content_for :title, 'Juvenile Parents Dashboard'
- @show_census = false
%h1= content_for :title
- if @html
  = render 'dashboards/tabs', active: 'parenting_children'
%p.text-muted
  Clients enrolled in homeless projects (ES, SH, SO, TH) where the client is under 18, listed as head of household, and the enrollment contains at least one other client under 18 but no one over 18.
