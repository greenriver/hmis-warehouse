- content_for :title, 'Individual Adults Dashboard'
- @show_census = false
%h1= content_for :title
= render 'dashboards/tabs', active: 'individual_adults'
%p.text-muted
  Clients enrolled in homeless projects (ES, SH, SO, TH) where the client is over 18 and the enrollment is not at a project with inventory for family.