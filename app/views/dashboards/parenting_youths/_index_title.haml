- content_for :title, 'Parenting Youth Dashboard'
- @show_census = false
%h1= content_for :title
= render 'dashboards/tabs', active: 'parenting_youth'
%p.text-muted
  Clients enrolled in homeless projects (ES, SH, SO, TH) where the client is between 18 and 24 years of age and the enrollment contains at least one other client under 18 but no one else over 24.