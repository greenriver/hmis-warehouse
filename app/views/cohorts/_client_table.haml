%table.table.table-condensed.datatable.cohort-client__table
  %thead
    %tr
      - if @first_name_column
        %th
          Last Name
      - if @last_name_column
        %th
          First Name
      - if @rank_column
        %th
          Rank
      %th
        Add Note
      %th.client_notes
        Notes
      - @visible_columns.each do |column|
        %th
          = column.title
      %th
        Remove
  %tbody
    - @cohort.cohort_clients.each do |cohort_client|
      = render 'client_table_row', cohort_client: cohort_client
