= content_for :modal_title, '3-month Follow Up'
= simple_form_for @follow_up, as: :follow_up, url: polymorphic_path(youth_follow_ups_path_generator) do |f|
  = render 'form', f: f
  = f.submit 'Save', class: 'btn btn-primary'