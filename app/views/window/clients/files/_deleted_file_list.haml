.table-responsive
  %table.table.table-sm
    %thead
      %tr
        %th Document
        %th Deletion Date
        %th Deletion Reason
      %tbody
        - files.each do |file|
          = render 'deleted_file_row', file: file
