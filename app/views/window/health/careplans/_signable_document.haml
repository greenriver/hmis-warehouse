:ruby
  team = careplan.team

= link_to polymorphic_path(careplan_path_generator, id: careplan) do
  %i.icon-eye
  View Our Latest Copy
%br

- if doc.signature_request_id.present?
  - if !doc.signed_by?('patient@openpath.biz')
    = link_to polymorphic_path([:signature] + careplan_path_generator + [:signable_document], {client_id: @client.id, careplan_id: careplan.id, id: doc.id, email: 'patient@openpath.biz', hash: doc.signer_hash('patient@openpath.biz'), post_sign_path: url_for, sign_out: true}) do
      %i.icon-exit
      Log out and get patient signature
    %br
