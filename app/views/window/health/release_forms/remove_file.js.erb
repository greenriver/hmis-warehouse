<% form = nil %>
<% simple_form_for @release_form, as: :form, url: form_url  do |f| %>
  <% form = f%>
<% end %>

var container = $('.j-file-upload');
container.empty();
container.append("<%= j render(partial: 'upload_fields', locals: {f: form}) %>");