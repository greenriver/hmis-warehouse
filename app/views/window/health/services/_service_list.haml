- if @services.blank?
  %p No services listed.
- else
  %table.table.service__list
    %thead
      %tr
        %th Type
        %th Provider
        %th Hours
        %th Days
        %th Requested On
        %th Effective Date
        %th End Date
        %th 


    %tbody
      - @services.each do |service|
        %tr
          %td= service.service_type
          %td= service.provider
          %td= service.hours
          %td= service.days
          %td= service.date_requested
          %td= service.effective_date
          %td= service.end_date
          %td
            = link_to polymorphic_path([:edit] + health_path_generator + [:service], id: service.id), data: { loads_in_pjax_modal: true }, class: 'btn-primary btn-muted btn-sm' do 
              %i.icon-pencil
              Edit
          