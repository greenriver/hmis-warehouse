<% form = nil %>
<% simple_form_for @form, url: upload_url  do |f| %>
  <% form = f%>
<% end %>

var container = $('.j-file-upload');
container.empty();
container.append("<%= j render(partial: 'upload', locals: {f: form}) %>");