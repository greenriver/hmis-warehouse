- title = "New Project Contact for #{@project.ProjectName}"
- content_for :title, title
%h1= title
.row
  .col-sm-4
    = simple_form_for(@contact, url: project_project_contact_path(@project, @contact)) do |f|
      = render 'form', f: f