.rollup-container.c-card.c-card--flush.mt-4
  %table.table.text-center
    %thead
      %tr
        - @scores.keys.each do |title|
          %th= title
    %tbody
      %tr
        - @scores.keys.each do |title|
          %td= @scores[title][medicaid_id]
