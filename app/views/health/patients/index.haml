- content_for(:title, "Patients for #{current_user.health_agency.name}")
%h1= content_for(:title)
= render 'search'
= render 'filter'
- if @patients.any?
  - @patients.each do |patient|
    = render 'patient_card', patient: patient