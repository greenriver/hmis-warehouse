- content_for :title, 'Add Site Manager Notification'
= render 'health/he/aliases'
= render 'health/he/tab_navigation', active: :site_managers
%h1= content_for :title

= simple_form_for @manager, url: health_he_case_site_managers_path do |f|
  = render 'form', f: f

  = f.submit "Add New Site Manager Notification"