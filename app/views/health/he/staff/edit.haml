- content_for :title, "#{GrdaWarehouse::Config.currrent_health_emergency_tracing_title} Contact Tracing"
%h1= content_for :title

= simple_form_for @staff, url: health_he_case_staff_path(@case, @staff) do |f|
  = render 'form', f: f

  = f.submit "Update Staff Member"