<% form = nil %>
<% simple_form_for @participation_form, as: :form, url: @remove_path  do |f| %>
  <% form = f%>
<% end %>

var container = $('.j-file-upload');
container.empty();
container.append("<%= j render(partial: 'upload_fields', locals: {f: form}) %>");