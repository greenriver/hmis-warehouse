- if !@hide_messages && user_signed_in? && current_user.in_app_messages?
  %span.messages.dropdown
    %a.btn.dropdow-toggle{ href: '#', role: 'button', data: { toggle: 'dropdown' } }
      %i.icon-envelope-o
        %span.message-count.badge.hide
    %ul.dropdown-menu.dropdown-menu-right
      %li
        = link_to messages_path, class: 'btn btn-default show-all' do
          show all messages

  - content_for :page_js do
    :javascript
      App.Users.Messages = new App.Users.Messages(#{poll_messages_path.to_json}, #{seen_messages_path.to_json});
