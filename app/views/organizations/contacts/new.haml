- title = "New Organization Contact for #{@entity.OrganizationName}"
- content_for :title, title
%h1= title
.row
  .col-sm-4
    = simple_form_for(@contact, as: :contact, url: organization_contacts_path(@entity)) do |f|
      = render 'form', f: f