= cache(['clients', 'other_enrollments', @client, can_view_projects?, controller_path, current_user], expires_in: GrdaWarehouse::Hud::Client::CACHE_EXPIRY) do
  = render 'enrollments', scope: @client.scope_for_other_enrollments(current_user)
