= cache(['clients', 'residential_enrollments', @client], expires_in: 4.hours) do
  = render 'enrollments', scope: @client.source_enrollments.residential.joins( project: :organization ).preload( :client, :exit, project: :organization ).order(EntryDate: :desc)