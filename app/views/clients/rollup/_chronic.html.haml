.jChronic-chart
  .jLoading Loading

:javascript
  $(document).ready(function() {
    chart = new App.ChartsScatterByDate.ChronicForClient(
      $('.jChronic-chart'), 
      '#{chronic_days_client_path(@client)}'
    );
    chart.load();
  });
  
%h4 Months homeless in the last 3 years: #{@client.months_served_since date: 3.years.ago.to_date}
%h4 Months homeless in the last year: #{@client.months_served_since date: 1.years.ago.to_date}
%h4 Homeless episodes in the last 3 years: #{@client.homeless_episodes_since date: 3.years.ago.to_date}
