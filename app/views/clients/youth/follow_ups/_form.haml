= f.input :contacted_on, as: :date_picker, label: 'Date of Follow Up'
= f.input :housing_status, collection: @follow_up.class.youth_housing_status_options, label: "Is The Youth Stably Housed?", input_html: { class: [:jHousingStatus] }, include_blank: false, as: :select_two
.jZipCode
  = f.input :zip_code, hidden: true, label: 'In What Zip Code?'

- content_for :page_js do
  :javascript
    $(function() {
      $('.jHousingStatus').on('change', function() {
        if ($('.jHousingStatus').val() != "No") {
          $('.jZipCode').show()
        } else {
          $('.jZipCode').hide()
        }
      });
      $('.jHousingStatus').trigger('change')
    })
