- files.each do |file|
  = render 'clients/files/file_row', file: file, consent: consent
- if paginate
  - if consent
    - anchor = 'consent'
  - else
    - anchor = 'other'
  .o-pagination.o-pagination--centered.mt-5
    = paginate files, params: { anchor: anchor }
