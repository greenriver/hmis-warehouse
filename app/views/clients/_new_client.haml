- if GrdaWarehouse::DataSource.available_for_new_clients.any? && params[:q].present? && can_create_clients?
  .slab
    .row
      .col-sm-6.pt-2.pb-4
        Don't see the client you are looking for?
      .col-sm-6.text-right.pt-2.pb-4
        = link_to polymorphic_path([:new] + client_path_generator), class: 'btn btn-secondary' do
          %span.icon-plus
          Add a Client
