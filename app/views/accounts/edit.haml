= content_for :title, 'Edit Account'
%h1= content_for :title
%hr
.row
  .col-sm-12
    = render 'accounts/account_info_tabs'
    = render 'form'
    - if Rais.env.development?
      = debug @user.provider_raw_info
