- breakdown = 'By CoC'
- sub_keys = @report.coc_bucket_titles.invert
- options = {coc: :true}
- chart_height = 800

- columns = [@report.enrolled_by_coc_data_for_chart[:columns]]
- if @report.include_comparison?
  - columns << @comparison.enrolled_by_coc_data_for_chart[:columns]
- categories = @report.enrolled_by_coc_data_for_chart[:categories]
= render 'performance_dashboards/overview/breakdown/enrolled', breakdown: breakdown, columns: columns, categories: categories, sub_keys: sub_keys, options: options, chart_height: chart_height

- columns = [@report.entering_by_coc_data_for_chart[:columns]]
- if @report.include_comparison?
  - columns << @comparison.entering_by_coc_data_for_chart[:columns]
- categories = @report.entering_by_coc_data_for_chart[:categories]
= render 'performance_dashboards/overview/breakdown/entering', breakdown: breakdown, columns: columns, categories: categories, sub_keys: sub_keys, options: options, chart_height: chart_height

- columns = [@report.exiting_by_coc_data_for_chart[:columns]]
- if @report.include_comparison?
  - columns << @comparison.exiting_by_coc_data_for_chart[:columns]
- categories = @report.exiting_by_coc_data_for_chart[:categories]
= render 'performance_dashboards/overview/breakdown/exiting', breakdown: breakdown, columns: columns, categories: categories, sub_keys: sub_keys, options: options, chart_height: chart_height


