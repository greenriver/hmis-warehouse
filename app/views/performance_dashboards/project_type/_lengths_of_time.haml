:ruby
  key = :length_of_time
  detail_params = @report.detail_params.merge({ key: key, length_of_time: true })
  columns = [@report.lengths_of_time_data_for_chart[:columns]]
  if @report.include_comparison?
    columns << @comparison.lengths_of_time_data_for_chart[:columns]
  end
  categories = @report.lengths_of_time_data_for_chart[:categories]


= render 'performance_dashboards/project_type/base', title: "Length of Time in #{@report.project_type_title}", key: key, sub_keys: @report.time_bucket_titles.invert, chart_height: 600, desc: 'Count of days served in enrollments occurring in the selected universe within 3 years of the end date for each client.', detail_params: detail_params, comparison_params: nil, columns: columns, categories: categories
