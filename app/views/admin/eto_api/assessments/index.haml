- title = 'ETO Touch Points'
- content_for :title, title
%h1= content_for(:title)
%table.table.table-condensed
  %thead
    %tr
      %th Site
      %th Touch Point
      %th Touch Point ID
      %th Active in ETO
      %th Fetch into Warehouse
      %th Display as confidential
      %th Exclude from Window
      %th Details visible in the window with release
      %th Health
  %tbody
    - @assessments.each do |assessment|
      %tr
        %td
          = assessment.site_name
        %td
          = link_to edit_admin_eto_api_assessment_path(assessment) do
            = assessment.name    
        %td
          = assessment.assessment_id
        %td
          = checkmark(assessment.active)
        %td
          = checkmark(assessment.fetch)
        %td
          = checkmark(assessment.confidential)
        %td
          = checkmark(assessment.exclude_from_window)
        %td
          = checkmark(assessment.details_in_window_with_release)
        %td
          = checkmark(assessment.health)
