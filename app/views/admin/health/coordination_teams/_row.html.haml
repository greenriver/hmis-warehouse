= simple_form_for team, url: admin_health_coordination_team_path(team) do |f|
  %tr
    %td= f.input :name, label: false
    %td= f.input :team_coordinator_id, collection: User.can_manage_care_coordinators.diet.order(last_name: :asc, first_name: :asc).distinct, as: :select_two, label: false
    %td
      %button.btn.btn-primary{name: :submit}
        %i.icon-pencil
        Save
    %td
      = link_to admin_health_coordination_team_path(team), method: :delete, class: 'btn btn-danger', data: { confirm: "Remove this team?" } do
        %i.icon-cross
          Remove