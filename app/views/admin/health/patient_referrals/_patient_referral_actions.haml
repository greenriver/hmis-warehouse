.col-sm-5
  = render 'assign_agency', pr: pr
%div{class: can_bulk_assign? ? 'col-sm-2' : 'col-sm-3'}
  = render 'reject', pr: pr
- if can_bulk_assign?
  .col-sm-1.d-flex
    .c-checkbox.c-checkbox.ml-auto
      - id = "patient_referral_id_#{pr.id}"
      = check_box_tag :patient_referral_id, pr.id, nil, id: id
      %label{for: id}
