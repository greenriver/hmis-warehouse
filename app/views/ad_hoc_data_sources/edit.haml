%h1 New Data Source
.row
  .col-sm-8
    = simple_form_for @data_source, url: ad_hoc_data_source_path(@data_source) do |f|
      = render 'form', f: f
      .form-actions
        = f.button :submit, value: 'Save Ad-Hoc Data Source', class: 'btn btn-primary'