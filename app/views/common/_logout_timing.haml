- if current_user.present?
  .site-status.border-top-1
    = Translation.translate('Session Expires at')
    %strong= current_user&.timeout_time(request.headers['HTTP_X_FORWARDED_ACCESS_TOKEN'])&.strftime('%l:%M:%S %P')
