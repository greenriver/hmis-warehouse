- if current_user.present?
  - expiry = user_session_expires_at
  - if expiry
    .site-status.border-top-1
      = Translation.translate('Session Expires at')
      %strong= expiry.strftime('%l:%M:%S %P')
